class check:
    def find(s):
        words=s.split()
        maxlength=0
        # for word in words:
            # maxlength= max(maxlength,len(word))
        print(max(words))
        return maxlength
print(check.find("hello guyes hoooooow"))